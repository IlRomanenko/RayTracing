//
// Created by ilya on 04.03.17.
//


#include "Material.h"
/*
ostream &operator<<(ostream &stream, const Material &material) {

    stream << "\tentry" << endl;
    stream << "\t\tname " << material.materialName << endl;
    stream << "\t\tcolor " << (u_int) (material.color.r * 255)
           << ' ' << (u_int) (material.color.g * 255)
           << ' ' << (u_int) (material.color.b * 255) << endl;

    stream << "\t\talpha " << material.alpha << endl;
    stream << "\t\treflect " << material.reflect << endl;
    stream << "\t\trefract " << material.refract << endl;
    stream << "\tendentry" << endl;

    return stream;
}
*/